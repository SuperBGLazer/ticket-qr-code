services:
  web:
    build: scanner/
    ports:
      - "0.0.0.0:5002:80"
    depends_on:
      - backend
  

  backend:
    build:
      context: server/
      dockerfile: Dockerfile
    ports:
      - "0.0.0.0:5003:5000"