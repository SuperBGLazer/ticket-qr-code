# Ticket QR code

This program dynamically generates ticket QR codes that rotate every 15 seconds.

## Installation

1.
```bash
pip install -r requirements.txt
```

2.
Linux
```bash
sudo apt install libzbar0
```

Mac
```bash
brew install zbar 
```